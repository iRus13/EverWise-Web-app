# Continued iOS and web quality work

This continues the full-app request after audit head `68a4bbe`. The original
live-account, provider, App Store/TestFlight and physical-device acceptance gaps
remain open. The overall goal is not complete.

## Repairs in this change

- Exercise data and learning players are loaded when an activity opens. A
  generated small catalog retains course order, badge names, completion content
  and progress requirements. A contract test rejects a stale catalog after
  curriculum edits. Authored lessons remain the only editable source of truth.
- A failed or stalled download offers retry, reload and return to the path.
  Timed-out, superseded and unmounted requests cannot replace the current screen
  or complete a lesson. Existing subscription checks still decide access before
  rendering learning content.
- Logos follow the configured build base. The install manifest and standalone
  privacy/terms return links work at both `/` and `/EverWise/`. Removed the Pages
  workflow's post-build string replacement; the build now produces correct paths.
- Added production browser checks to the PR workflow, covering both hosting
  paths, public login validation, all three deferred player types, image and
  manifest loading, and startup size limits.

## Performance evidence

The same local production build changed initial JavaScript from **1,907.14 KB**
to **1,022.58 KB**, about **46% less** before compression. Vite reports initial
gzip size changing from **514.01 KB** to **299.07 KB**, about **42% less**.
The exercise module is a separate 956.14 KB chunk (232.47 KB gzip). It is not
requested on the welcome screen. Some small metadata is present in both chunks;
this change reduces startup transfer, not total bytes after opening lessons.

Production WebKit checks at 390px and 1440px verify both hosting bases. The
script independently calculates gzip sizes using Node's default compression,
which differs slightly from Vite's compression settings. No real-network speed
or physical-device launch-time claim is made.

## Validation and limitations

- 90 WebKit layout combinations and both public onboarding journeys pass.
- All 1,680 UI tests pass, including 208 focused account/billing/navigation/deferred-content cases.
- Lint and catalog consistency tests pass.
- Updated web assets sync to Capacitor; iPhone 18 Pro / iOS 27 simulator build,
  install and launch pass. The welcome screen was visually checked. Semantic
  automation still does not expose its buttons reliably; this is not native
  interaction, keyboard, VoiceOver or purchase proof.
- The initial full local unit run passed 435/441 cases. Its six failures were
  browser/server process-startup timeouts under heavy concurrent system load,
  before the related application assertions. All 48 cases in the two affected
  files pass on recheck, including all Chromium layouts. Final CI runs the complete
  suite again. No timeouts or assertions were relaxed for this run.
- Runtime configuration and the local dependency-script approval map are unchanged.

## Open work

1. Add password recovery from the login screen for email-based accounts. Public
   username-only accounts use a reserved non-deliverable auth domain and require
   a separate recovery design; do not imply an email was delivered to that alias.
2. Exercise offline progress persistence and failed saves across reload/retry.
3. Continue keyboard/focus and native interaction checks where the tools expose
   usable controls.
4. Obtain the dedicated Firebase/API/Apple test setup to verify real account,
   provider and subscription lifecycles. The MacBook task is currently unreachable
   from this host; a setup-location question is pending. Do not place secrets in
   reports or task messages.

No merge, deployment, production data change, account creation or purchase is
included in this repair.

## Exact follow-up file manifest

- `.github/workflows/deploy-pages.yml`
- `.github/workflows/qa.yml`
- `README.md`
- `docs/qa/2026-09-19-followup.md`
- `public/manifest.webmanifest`
- `public/privacy.html`
- `public/terms.html`
- `scripts/build-course-catalog.mjs`
- `scripts/qa-production.mjs`
- `src/App.jsx`
- `src/components/AppShell.jsx`
- `src/components/LearningContent.jsx`
- `src/components/PartnerBrand.jsx`
- `src/data/course-catalog.js`
- `src/data/lessons.js`
- `src/data/scam-phase3-lessons.js`
- `src/screens/Home.jsx`
- `src/screens/Landing.jsx`
- `src/screens/LessonPath.jsx`
- `src/screens/Loading.jsx`
- `src/screens/PartnerDashboard.jsx`
- `src/screens/Paywall.jsx`
- `src/screens/PersonalPlan.jsx`
- `src/screens/learningScreens.jsx`
- `src/utils/badges.js`
- `tests/app-navigation.test.jsx`
- `tests/billing-client.test.jsx`
- `tests/course-catalog.test.js`
- `tests/learning-content.test.jsx`
- `tests/partner-client.test.jsx`
